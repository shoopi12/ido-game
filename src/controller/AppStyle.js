import { StyleSheet } from 'react-native';

export const Style = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'black',
    },
    flatList: {
        width: '90%',
        alignSelf: 'center',
    },
});
